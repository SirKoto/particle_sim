#version 430
layout(local_size_x = 32, local_size_y = 1) in;

#include "../shader_includes/particle_types.in"

layout(std430, binding = 0) buffer ParticleData
{
    Particle particles[];
};

void main() {
    uint idx = gl_GlobalInvocationID.x;

    particles[idx].pos.x += 0.01;
}
